# Supabase configuration.
# Copy to .env.local and fill in to connect the platform to your Supabase
# project. When these are unset, CITYPULSE runs in DEMO MODE with a built-in
# live simulation engine (same UI, mock data).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=